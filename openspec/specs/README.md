# OpenSpec Specs

Source-of-truth specs live under `{domain}/spec.md`.
Each domain documents stable requirements for that area of the aion-landing app
(UI sections, components, hooks, i18n, and the contact endpoint).